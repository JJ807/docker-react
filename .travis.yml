services:
  - docker

before_install:
  - docker build -t jj/docker-react -f Dockerfile.dev .

script:
  - docker run jj/docker-react npm run test -- --coverage

after_script:
  - exit 0

after_success:
  - echo "hurray!"
